// index.tsx — Entry point mapFIRE (Expo SDK 54, TypeScript)
// react-native-gesture-handler HARUS di-import pertama

import 'react-native-gesture-handler';
import { registerRootComponent } from 'expo';
import App from './App';

registerRootComponent(App);
